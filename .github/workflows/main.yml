name: MyTest

on:
  push:
    branches:
      - master


jobs:
  job1: 
    name: test job1
    runs-on: ubuntu-latest
    steps:
      - name: step1
        env:
          ENV: develop
        run: |
          echo $ENV
