name: MyTest

on:
  push:
    branches:
      - main


jobs:
  job1: 
    name: test job1
    runs-on: ubuntu-latest
    steps:
      - name: step1
        env:
          ENV: develop
        run: |
          node -v
