name: MyTest

on:
  push:
    branches:
      - main


jobs:
  job1: 
    name: test job1
    runs-on: ubuntu-latest
    steps:
      - name: pull code
        env:
          ENV: develop
        uses: actions/checkout@v1
      - name: exec process
        run: |
          node ./ci/test.js
        
