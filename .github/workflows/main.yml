name: MyTest

on:
  push:
    branches:
      - main


jobs:
  job1: 
    name: test job1
    runs-on: ubuntu-latest
    steps:
      - name: step1
        uses: actions/checkout@v1
        env:
          ENV: develop
        run: |
          node ./ci/test.js
